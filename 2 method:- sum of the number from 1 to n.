// sum of the number from 1 to n.

// using while loop
// module tab;
   
//   int sum, count;
//   int a=5;
  
//   initial begin
//     while(count <=a)begin
//       sum =sum+count;
//     count = count +1;
    
//     end
//     $display("sum=%0d", sum);
//   end
// endmodule

// try other way :- using  for loop

module tab;
   
  int sum, count;
  int a=5;
  
  initial begin
    for(int i=0; i<=a; i++)begin
      sum =sum+count;
    count = count +1;
    
    end
    $display("sum=%0d", sum);
  end
endmodule
