module tab;
   int a;
  
  
  function check(int a);
    if(a%2==0)
      $display(" even");
    else
      $display(" odd");
  endfunction
  
  initial begin
    check(17);
    check(18);
    check(77);
  end
endmodule 
