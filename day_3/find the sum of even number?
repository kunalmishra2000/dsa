// find the sum of even number?
module tab;
   int  a=6;
  int sum;
  int count=0;
  function void display();
    for(int i=0; i<a; i++)begin
      if(a>count)begin
        if(count%2==0)begin
        sum += count;
       
    end
        count++;
      end
    end
    $display("count=%0d  sum_of_even_number=%0d a=%0d", count, sum,a);
  endfunction 
    
  initial begin
    display();
  end
  
endmodule 
  
